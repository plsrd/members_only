extends layout

block content
  div 
    if messages  && messages.length > 0
      for message in messages 
        p !{message.title}
        p !{message.content}
        p #{message.age}
    else 
      p no messages